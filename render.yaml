services:
  - type: web
    name: ecotroc
    runtime: node
    buildCommand: npm install
    startCommand: npm run init-db && node server.js
    nodeVersion: "20"
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
